import{beforeEach,expect,test}from"vitest";import{exportBrowserState,migrationKeys}from"./browserStateExport";
beforeEach(()=>localStorage.clear());
test("quarantines an invalid sibling and hashes every recognized category",async()=>{const valid={id:"group-1",name:"AI",symbols:["NVDA"]};localStorage.setItem("stock_m:watchlists:v1",JSON.stringify([valid,{id:42}]));localStorage.setItem("stock_m:performance-cache:v1","secret-cache");const result=await exportBrowserState(localStorage,()=>"2026-08-10T10:00:00.000Z");expect(result.document.categories["stock_m:watchlists:v1"].valid).toEqual([valid]);expect(result.document.categories["stock_m:watchlists:v1"].quarantined).toHaveLength(1);expect(result.document.manifest.sha256).toMatch(/^[a-f0-9]{64}$/);expect(Object.keys(result.document.categories)).toEqual([...migrationKeys]);expect(result.document.rawStorage).not.toHaveProperty("stock_m:performance-cache:v1");expect(result.backupStored).toBe(true);expect(result.download).toBeInstanceOf(Blob);});
