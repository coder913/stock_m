import {expect,test,vi} from "vitest";import {ReconciliationService} from "./reconciliationService";
test("records drift and clears it only after a later complete successful reconciliation",async()=>{const provider={getAccount:vi.fn().mockResolvedValueOnce({cash:"100",buyingPower:"100",equity:"100",portfolioValue:"100",accountId:"a",status:"ACTIVE",currency:"USD",tradingBlocked:false,accountBlocked:false,createdAt:"x",observedAt:"x"}).mockResolvedValueOnce({cash:"90",buyingPower:"90",equity:"100",portfolioValue:"100",accountId:"a",status:"ACTIVE",currency:"USD",tradingBlocked:false,accountBlocked:false,createdAt:"x",observedAt:"x"}),listPositions:vi.fn().mockResolvedValue([]),listOpenOrders:vi.fn().mockResolvedValue([]),listActivities:vi.fn().mockResolvedValue([])};const repository={replay:vi.fn().mockResolvedValue({cash:"90.00000000",positions:[],provenance:[]}),saveSnapshot:vi.fn(),observeOrder:vi.fn(),ingestActivities:vi.fn(),recordReconciliation:vi.fn().mockResolvedValue("run"),replaceDrift:vi.fn(),clearDrift:vi.fn()};const service=new ReconciliationService(provider as never,repository as never,()=>new Date("2026-08-11T00:00:00Z"));await service.reconcileAll();expect(repository.replaceDrift).toHaveBeenCalledWith(expect.objectContaining({cashDifference:"10.00000000"}));await service.reconcileAll();expect(repository.clearDrift).toHaveBeenCalled();});
