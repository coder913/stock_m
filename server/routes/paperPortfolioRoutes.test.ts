// @vitest-environment node
import{expect,test,vi}from"vitest";import{buildApp}from"../app";const config={host:"x",port:1,providers:{alpaca:{configured:true},sec:{configured:true},finnhub:{configured:false},fred:{configured:false}},publicStatus:{providers:{}}};test("exposes isolated Paper overview with provenance and active drift",async()=>{const store={getOverview:vi.fn().mockResolvedValue({account:{cash:"100"},positions:[],drift:{cashDifference:"10",symbolDifferences:[]},asOf:"2026-08-11T00:00:00Z"}),listOrders:vi.fn(),getOrderTimeline:vi.fn(),listActivities:vi.fn(),listLedger:vi.fn()};const app=buildApp({config,cache:{health:async()=>({writable:true,entries:0})},paperPortfolio:{store,outbox:{append:vi.fn()},database:{transaction:()=>({execute:(fn:Function)=>fn({})})} as never,idempotency:{execute:vi.fn(async(_tx,_key,_fp,command)=>command())} as never}});const response=await app.inject({method:"GET",url:"/api/v1/portfolio/alpaca-paper"});expect(response.statusCode).toBe(200);expect(response.json()).toMatchObject({source:"alpaca-paper",drift:{cashDifference:"10"}});await app.close();});
