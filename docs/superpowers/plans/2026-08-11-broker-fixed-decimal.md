# 券商金额定点精度实施计划

1. 用失败测试固定 Decimal 解析、格式、乘法、舍入和非法输入规则。
2. 实现可复用的 BigInt 可配置定点工具（数量 9 位、金额与价格 8 位）。
3. 用失败测试复现活动映射的浮点尾差和安全整数丢失。
4. 将 FILL 金额计算改为 BigInt 定点乘法并规范买卖符号。
5. 将 Paper 账本回放、订单预览与差额计算迁移到统一工具。
6. 增加迁移，将券商数量列扩展为 `numeric(29,9)`。
7. 运行定向测试、完整测试、类型检查与生产构建。
8. 审查、提交并合并到 `main`。
