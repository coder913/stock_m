// @vitest-environment node
import{readFileSync}from"node:fs";import{expect,test,vi}from"vitest";import{runLiveSmoke}from"./liveSmoke";test("unconfigured providers are skipped without infrastructure connections",async()=>{const output=vi.fn();await runLiveSmoke({},output);expect(output.mock.calls.flat().join(" ")).toContain("alpaca-paper: skipped");});test("live smoke source has no broker mutation calls",()=>{const source=readFileSync(new URL("./liveSmoke.ts",import.meta.url),"utf8");const forbidden=["submit"+"Order","cancel"+"Order"];for(const name of forbidden)expect(source).not.toContain(name);});
