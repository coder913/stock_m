import{render,screen}from"@testing-library/react";import{expect,test,vi}from"vitest";import{PaperPortfolioOverview}from"./PaperPortfolioOverview";test("renders only broker-backed Paper account and positions",async()=>{const api={getOverview:vi.fn().mockResolvedValue({source:"alpaca-paper",account:{cash:"100",buyingPower:"200",equity:"300",portfolioValue:"300",observedAt:"2026-08-11"},positions:[{symbol:"AAPL",quantity:"2",marketValue:"400",averageEntryPrice:"100",observedAt:"2026-08-11"}],asOf:"2026-08-11"}),listOrders:vi.fn().mockResolvedValue([]),getTimeline:vi.fn(),reconcile:vi.fn()};render(<PaperPortfolioOverview api={api}/>);expect(await screen.findByText("AAPL")).toBeInTheDocument();expect(screen.getAllByText("100")).toHaveLength(2);expect(screen.queryByText("手工组合")).not.toBeInTheDocument();});
