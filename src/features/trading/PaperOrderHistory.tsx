import{useEffect,useState}from"react";import type{PaperOrderView,PaperPortfolioApi}from"./paperPortfolioApiClient";export function PaperOrderHistory({api}:{api:PaperPortfolioApi}){const[orders,setOrders]=useState<PaperOrderView[]>([]);useEffect(()=>{void api.listOrders().then(setOrders);},[api]);return <section><h2>Paper 订单</h2><table aria-label="Paper 订单历史"><thead><tr><th>股票</th><th>方向</th><th>数量</th><th>状态</th></tr></thead><tbody>{orders.map(order=><tr key={order.id}><th>{order.symbol}</th><td>{order.side}</td><td>{order.quantity}</td><td>{order.state}</td></tr>)}</tbody></table>{!orders.length&&<p>暂无 Paper 订单</p>}</section>}
