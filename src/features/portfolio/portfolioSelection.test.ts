import{beforeEach,expect,test}from"vitest";import{loadPortfolioSelection,savePortfolioSelection}from"./portfolioSelection";beforeEach(()=>localStorage.clear());test("manual is the default and Paper is unavailable when disabled",()=>{expect(loadPortfolioSelection(false)).toBe("manual");localStorage.setItem("stockm.portfolio.selection","alpaca-paper");expect(loadPortfolioSelection(false)).toBe("manual");});test("persists only the non-business UI preference",()=>{savePortfolioSelection("alpaca-paper");expect(localStorage.getItem("stockm.portfolio.selection")).toBe("alpaca-paper");});
