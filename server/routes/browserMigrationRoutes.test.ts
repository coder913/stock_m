// @vitest-environment node
import{expect,test,vi}from"vitest";import{buildApp}from"../app";
test("previews and imports a migration document",async()=>{const service={preview:vi.fn(async()=>({targetEmpty:true})),import:vi.fn(async()=>({id:"receipt-1"})),latestReceipt:vi.fn(async()=>undefined)};const app=buildApp({config:{host:"127.0.0.1",port:1,providers:{alpaca:{configured:false},sec:{configured:true},finnhub:{configured:false},fred:{configured:false}},publicStatus:{providers:{}}},cache:{health:()=>({writable:true,entries:0})},browserMigration:{service}as never});expect((await app.inject({method:"POST",url:"/api/v1/migration/preview",headers:{"idempotency-key":"preview-1"},payload:{version:1}})).statusCode).toBe(200);expect((await app.inject({method:"POST",url:"/api/v1/migration/import",headers:{"idempotency-key":"import-1"},payload:{version:1}})).statusCode).toBe(201);await app.close();});
