import type {BrokerActivity} from "../../shared/broker";
export interface BrokerLedgerInput{remoteSourceId:string;eventType:"buy"|"sell"|"dividend"|"fee"|"split"|"deposit"|"withdrawal"|"unknown";symbol?:string;amount?:string;quantity?:string;price?:string;quantityMultiplier?:string;occurredAt:string;provenance:unknown;}
export function mapBrokerActivity(activity:BrokerActivity):BrokerLedgerInput{const type=activity.type.toUpperCase();let eventType:BrokerLedgerInput["eventType"]="unknown";if(type==="FILL"&&activity.side)eventType=activity.side;else if(type==="DIV"||type==="DIVIDEND")eventType="dividend";else if(type.includes("FEE"))eventType="fee";else if(type==="SPLIT")eventType="split";else if(type==="CSD"||type==="DEPOSIT")eventType="deposit";else if(type==="CSW"||type==="WITHDRAWAL")eventType="withdrawal";const amount=activity.amount??(eventType==="buy"&&activity.quantity&&activity.price?`-${Number(activity.quantity)*Number(activity.price)}`:eventType==="sell"&&activity.quantity&&activity.price?String(Number(activity.quantity)*Number(activity.price)):undefined);return{remoteSourceId:`activity:${activity.remoteActivityId}`,eventType,symbol:activity.symbol,amount,quantity:activity.quantity,price:activity.price,quantityMultiplier:eventType==="split"?activity.quantity:undefined,occurredAt:activity.occurredAt,provenance:activity.raw};}
