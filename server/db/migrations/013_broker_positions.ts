import{sql,type Kysely}from"kysely";export async function up(database:Kysely<unknown>){await sql`create table broker.position_snapshot(symbol text primary key,quantity numeric(28,8) not null,market_value numeric(28,8) not null,average_entry_price numeric(28,8) not null,observed_at timestamptz not null)`.execute(database);}export async function down(database:Kysely<unknown>){await sql`drop table if exists broker.position_snapshot`.execute(database);}
