import{render,screen}from"@testing-library/react";import{expect,test,vi}from"vitest";import{PaperOrderHistory}from"./PaperOrderHistory";test("renders broker order projection states",async()=>{const api={listOrders:vi.fn().mockResolvedValue([{id:"1",symbol:"NVDA",side:"buy",quantity:"1",state:"partially_filled",updatedAt:"x"}])};render(<PaperOrderHistory api={api as never}/>);expect(await screen.findByText("partially_filled")).toBeInTheDocument();});
